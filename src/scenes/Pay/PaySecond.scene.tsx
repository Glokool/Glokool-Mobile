import React from 'react';
import {Text, StyleSheet} from 'react-native'
import { Layout } from '@ui-kitten/components';

export const PaySecondScene = () => {
    return (
        <Layout></Layout>
    )
};